{
    "options": {
        "env": {
            "GNU_INSTALL_ROOT": "${config:GNU_INSTALL_ROOT}",
            "SDK_ROOT": "${config:SDK_ROOT}"
        }
    },
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "make",
            "problemMatcher": {
                "base": "$gcc",
                "fileLocation": [
                    "relative",
                    "${workspaceRoot}/_build"
                ]
            }
        },
        {
            "label": "flash",
            "type": "shell",
            "command": "make flash",
            "problemMatcher": {
                "base": "$gcc",
                "fileLocation": [
                    "relative",
                    "${workspaceRoot}/_build"
                ]
            }
        },
        {
            "label": "clean",
            "type": "shell",
            "command": "make clean",
            "problemMatcher": {
                "base": "$gcc",
                "fileLocation": [
                    "relative",
                    "${workspaceRoot}/_build"
                ]
            }
        },
    ]
}