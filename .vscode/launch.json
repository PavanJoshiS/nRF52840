{
    "configurations": [
        {
            "name": "jlink",
            "cwd": "${workspaceRoot}",
            "executable": "${workspaceRoot}/_build/build.elf",
            "request": "launch",
            "type": "cortex-debug",
            "servertype": "jlink",
            "device": "nrf52",
            "interface": "swd",
            "preLaunchTask": "build",
            "svdFile": "${config:SDK_ROOT}/modules/nrfx/mdk/nrf52840.svd",
            "cortex-debug.gdbPath": "${config:GDB_PATH}"
        },
    ]
}